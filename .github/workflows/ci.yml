name: Machine Learning Workflow CI Advance

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  train-and-push-docker:
    runs-on: ubuntu-latest

    steps:
    # 1. Ambil source code dari repository GitHub
    - name: Checkout repository code
      uses: actions/checkout@v3

    # 2. Pasang Python versi 3.12 di server GitHub
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    # 3. Install semua library yang dibutuhkan
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install mlflow==2.19.0 scikit-learn pandas numpy

    # 4. Jalankan training model lokal dan save ke folder statis
    - name: Run MLflow Project Retraining
      run: |
        cd MLProject
        rm -rf my_model
        python modelling.py

    # 5. Login otomatis ke Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # 6. Build model dari folder 'my_model' dan Push ke Docker Hub
    - name: Build and Push MLflow Docker Image
      run: |
        cd MLProject
        mlflow models build-docker -m "my_model" -n "aliyapradnya/crop_recommendation_model:latest" --enable-mlserver
        docker push aliyapradnya/crop_recommendation_model:latest